# config/services.yaml
parameters:
    fraud_api_url: '%env(FRAUD_API_URL)%'

    twilio_sid: '%env(TWILIO_SID)%'
    twilio_token: '%env(TWILIO_TOKEN)%'
    twilio_whatsapp_from: '+14155238886'
    
    products_images_directory: '%kernel.project_dir%/public/uploads/products'

services:
    _defaults:
        autowire: true      
        autoconfigure: true 
        
        bind:
            $fraudApiUrl: '%fraud_api_url%'
            $apiKey: '%env(GROQ_API_KEY)%'

    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # --- Services Spécifiques (On garde TOUT) ---

    App\Service\StripeService:
        arguments:
            $stripeSecretKey: '%env(STRIPE_SECRET_KEY)%'

    App\Security\LoginSuccessHandler:
        arguments:
            $router: '@router'

    # Service de tes collègues pour la Blockchain
    App\Service\SmartContractGenerator:
        autowire: true

    # Configuration des contrôleurs
    App\Controller\BackOffice\:
        resource: '../src/Controller/backOffice/'
        tags: ['controller.service_arguments']